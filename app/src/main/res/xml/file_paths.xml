<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- QR-коды подписки, которыми делится владелец -->
    <cache-path name="shared" path="shared/" />
    <!-- Скачанное обновление: отсюда его читает системный установщик -->
    <cache-path name="updates" path="updates/" />
    <!-- Журнал, который прикладывают к обращению в поддержку -->
    <cache-path name="support" path="support/" />
</paths>
